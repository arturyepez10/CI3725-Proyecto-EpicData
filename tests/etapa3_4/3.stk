# CASOS BORDE
type([])
[1, 2, [1, 2]]
[0, 1, 2][2.5]

# Definir arreglo usando inferencia
[num] x := [];
# Ver su tipo
type(x)

# inferencia indirecta
[bool] y := if(false, [], [1, 2]);
[bool] y := if(false, [], []);
type(y)

# Tipo de funciones
ltype([1, 2])
type(pi)
type(length)

# Tipo de type
type(type(type(1)))

# Lo mismo con ltype
ltype(pi)
ltype(length)
ltype(ltype(ltype(1)))

# Añadir
# [1, 2, 3][1] := [4] es posible en Stokhos ??